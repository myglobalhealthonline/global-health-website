# Portal Overlay & Stacking Inventory

Audit date: 2026-07-11 · Static analysis. Verdicts: Yes = mechanism confirmed / Latent = safe today, fragile / No = verified safe / Unverified = needs runtime.

Layer tokens referenced: `../shared/responsive-design-system-plan.md` §1.2.

| ID | Scope | Route | File | Component | Overlay type | Portalled? | Effective layer / z-index | Clipping ancestor(s) | Stacking-context creator(s) | Fails? | Reproduction | Root cause | Fix (token/portal/structure) | Severity |
|----|-------|-------|------|-----------|--------------|------------|---------------------------|----------------------|------------------------------|--------|--------------|------------|-------------------------------|----------|
| PO-01 | All portals | shell | components/PortalDialog.tsx:15-100 + portal.css:5200-5239 | PortalDialog (modal / mobile bottom-sheet) | Modal | **No** — in-tree `fixed inset:0` div, NOT createPortal | z-50 (`.gh-portal-dialog-overlay` portal.css:5203) → `--z-modal` | none active — verified no transform/filter/will-change between all 10 call sites and body | `.gh-portal-topbar` backdrop-filter exists but dialogs render in `<main>` | **Latent** | nest any future PortalDialog consumer under a `gh2-glass-*`/backdrop-filter wrapper → dialog positions/clips inside that ancestor (containing-block rule for fixed) | full-viewport fixed overlay without DOM portal in a glass-heavy codebase (38+13 backdrop-filter uses) | migrate internals to `createPortal(…, document.body)` (pattern proven at consultation-documents-modal.tsx:815); keep API | Medium (High blast radius if triggered) |
| PO-02 | All portals | shell topbar | components/NotificationPopover.tsx:44-250 | Notification popover | Custom dropdown | **No** | content z-40, catcher z-30 (:121,125) → `--z-dropdown` | `.gh-portal-topbar` has no overflow-hidden — safe | topbar backdrop-filter (portal.css:4524-4527) — popover is `absolute`, unaffected | **Latent + Z-C1** | see PO-05 | sibling z-40 tie with sidebar inside isolate shell | Radix AppMenu, `--z-dropdown` | Medium |
| PO-03 | All portals | shell topbar | portal-shell.tsx:396-457 AND admin-shell.tsx:584-634 | User menu (duplicated verbatim ×2) | Custom dropdown | **No** | content z-40, catcher z-30 | none | same as PO-02 | **Latent + Z-C1** | same | duplicate hand-rolled implementation | dedupe into one AppMenu component | Medium |
| PO-04 | Admin | topbar | (admin)/admin/_components/country-picker.tsx:47-97 | Country picker | DropdownMenu (Radix) | **Yes** (Portal :60) | z-50 (:64) → `--z-dropdown` | none (body-mounted) | — | No | — | — | token mapping only; fix item `outline-none` focus (:84) | Low |
| PO-05 | All portals | shell | portal-shell.tsx:232,289 / admin-shell.tsx:371,476 + portal.css:4110-4113 | Sidebar (fixed z-40) vs topbar (sticky z-20) vs popovers (z-40) | Shell layering | n/a | sidebar `--z-header`; popovers `--z-dropdown` | n/a | `.gh-portal-shell { isolation: isolate }` (portal.css:4112) = one shared stacking context | **Yes (fragile-by-DOM-order)** | Z-C1: popover z-40 ties sidebar z-40; popover wins only because its markup follows `<aside>` in JSX — any reorder silently flips paint order. Unverified visually (needs 1024–1280px screenshot with popover reaching sidebar edge) | two layer roles share one z value inside one isolated context | token split: header band 200 vs dropdown 300 | Medium |
| PO-06 | Doctor | appointment workspace | (doctor)/doctor/appointments/[id]/_components/consultation-documents-modal.tsx:815 | Documents modal | Modal | **Yes** — `createPortal(modal, document.body)` (only true createPortal in repo) | fixed, own classes → `--z-modal` | none | — | No (layering) / **Unverified** (focus trap — Escape confirmed :297, trap not found in grep) | Tab out of open modal → focus may escape to page | separate one-off modal implementation | fold into AppDialog; add trap | Medium (a11y) |
| PO-07 | Patient | /account/security | (auth)/account/security/_components/delete-account-button.tsx:99-150 | Delete-account modal | Modal | **No** | fixed, `max-h-[calc(100vh-2rem)]` (:123, correct) | none | none | **Yes (a11y, static-confirmed)** | keyboard: open modal, Tab repeatedly → focus leaves modal into page (no trap, no initial focus, no restore; aria attrs present :119-122) | un-migrated custom modal (PortalDialog doc-comment names it as migration target) | migrate to AppDialog/PortalDialog | Medium |
| PO-08 | Admin | shell | admin-shell.tsx:33,670-680 + query-toast.tsx | sonner Toaster | Toast | Yes (library-internal portal) | library z, unreconciled → `--z-toast` | none | — | **Unverified** | fire toast while PortalDialog open → stacking order library-determined | toast layer never reconciled with app z-50 ceiling | set `--z-toast` via Toaster style/toastOptions; runtime-verify above modal | Low-Medium |
| PO-09 | All portals | shell mobile | portal-shell.tsx:224 / admin-shell.tsx:366 | Sidebar scrim | Overlay backdrop | No (fine — full-viewport fixed at shell root) | z-30 | none | shell isolate | No | — | — | pair with `--z-header` band | Low |
| PO-10 | Admin | doctors availability | doctors/[id]/availability/_components/book-slot-dialog.tsx | Book-slot dialog | Modal (PortalDialog) | inherits PO-01 | inherits | inherits | inherits | Latent (via PO-01) | — | — | fixed by PO-01 | Low |
| PO-11 | Shared | calendars | components/calendar/EventDetailDialog.tsx | Event detail dialog | Modal (PortalDialog) | inherits PO-01 | inherits | inherits | inherits | Latent | — | — | fixed by PO-01 | Low |
| PO-12 | Admin | tables | portal.css:4153-4156 `.gh-admin-table-wrap { overflow: hidden }` | Table wrapper | Clipping container | n/a | n/a | IS the clipping ancestor | none | **No today** — row actions are inline buttons, no absolute menus inside; becomes a trap the day kebab menus are added un-portalled | — | wrapper overflow policy | rule (design-system §1.4): kebab menus must be portalled AppMenu | Low (guard rail) |
| PO-13 | Admin | /admin/audit-log | audit-log/page.tsx:324 | Ops table wrapper | Clipping container | n/a | n/a | `overflow-hidden` on the SCROLL wrapper (only one in repo; siblings use overflow-x-auto) | none | **Yes** — content clipping (data loss view), not overlay | wide metadata `<details>` diffs + IP/entity columns clipped with no scroll access at container < table width | wrong overflow keyword on one wrapper | `overflow-hidden` → `overflow-x-auto` | **High** |
| PO-14 | All portals | shell | portal.css:4116-4123 `.gh-portal-shell::before` | Decorative bloom | Fixed decoration | n/a | z:-1 | n/a | fixed layer | No | — | — | none | Low |
| PO-15 | Portals | strips | portal.css:20-25 (comment: overflow-x-only computes overflow-y:auto) | Horizontal scroll strips (PortalTabs etc.) | Scroll strip | n/a | — | strip = clip for absolute children | — | No today — PortalTabs (portal.css:5106-5127) contains only buttons + mask-fade affordance | — | — | rule: no un-portalled menu triggers inside scroll strips | Low |
| PO-16 | Admin | appointments | appointments/_components/manual-booking-form.tsx | Booking form dropdowns | Unknown | **Unverified** | grep-matched z- classes, not read | — | — | Unverified | read file; open dropdowns near container edges | — | audit follow-up | Unverified |
| PO-17 | Portals (patient) | bookings/family/membership | PortalDialog consumers (bookings/ui.tsx:609,621 width="sm", FamilyPanel, ManagePanel, order-actions, prescriptions-list, availability-ui, admin-orders-table, ChatThread) | Various dialogs | Modal (PortalDialog) | inherits PO-01 | z-50; widths sm=560px lg=760px full (portal.css:5214-5239); mobile bottom-sheet ≤ media query (portal.css:5313-5317) | inherits | inherits | Latent (via PO-01) | — | — | fixed by PO-01 | Low |

## Portal stacking-context creator map

| Creator | File:line | Effect |
|---|---|---|
| `.gh-portal-shell { isolation: isolate }` | portal.css:4110-4113 | one stacking context for sidebar/topbar/main/dialogs — makes all z ties DOM-order-resolved (Z-C1 root) |
| `.gh-portal-topbar` backdrop-filter | portal.css:4524-4527 | containing block for fixed descendants (none today); absolute popovers unaffected |
| Sidebar `translate-x` slide | portal-shell.tsx:232 / admin-shell.tsx:371 | transform on the sidebar itself — contains only sidebar content, safe |
| `@supports not (backdrop-filter)` fallback | portal.css:4541-4545 | solid bg swap — layering must not depend on blur presence (it doesn't) |
| `.gh-admin-table-wrap` overflow:hidden | portal.css:4153-4156 | clip boundary — see PO-12 rule |

## Verification steps (runtime, pending)

1. PO-05: 1024–1280px, open NotificationPopover + user menu; screenshot vs sidebar edge; then JSX-reorder test in a scratch branch to confirm DOM-order dependence.
2. PO-07: keyboard Tab-cycle in delete-account modal (confirm escape-from-modal), repeat for PO-06 documents modal.
3. PO-08: fire toast with PortalDialog open; assert toast above modal.
4. PO-13: audit-log with metadata diff wider than container at 800px — confirm clipped vs scrollable.
5. All modals: open each PortalDialog consumer at 320px/200% zoom — assert within viewport, internal scroll, body scroll locked.
