# Spain measurement evidence and cohort rules

Baseline: final GSC July 16–August 12 versus August 13–September 9, 2026.
Use page totals for cohort totals; query/page rows for intent diagnosis. Keep the
Spain-resident cut separate from all-country traffic and page locale separate from
spoken consultation languages. Never sum or average average-position values into
a fabricated cohort position.

At actual publication, record the exact changed URLs, group hashes, mutation/public
receipts, source commit and deployment IDs in the canonical ledger. Start 30/60/90
review dates then; no dates are scheduled from this research package. Use complete,
final matched 28-day windows, excluding the partial publication day where practical.

Compare service and profile clicks, impressions and query mix separately from
articles, tools, home/care hubs and legacy URLs. Revisit ES dermatology queries
landing on EN before changing locale targeting. Report retained/held URLs separately
from actual changed cohorts. An HTTP/FAQ check proves rollout integrity, not ranking
improvement.

GA4 collection resumed September 9 (ledger §47). Validate consented service-to-booking
events and completion instrumentation before interpreting consultation conversion;
earlier missing collection is not recoverable and this task made no paid booking.

Keep the September 18 Spain read in §53 and September 24 global inspection unchanged.
No tracker, automation or indexing submission was created.
