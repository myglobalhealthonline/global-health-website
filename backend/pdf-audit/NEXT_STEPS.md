# Next Steps — applying the approved PDF design system

Nothing below is done yet. Production is untouched (one export-only change: `buildInvoiceHtml` exported from `invoice-pdf.ts` for sample tooling).

## Step 0 — Approval gate
Review `proposed-template/proposed-invoice-preview.png` / `.pdf`. Approve or request changes. Everything below waits on this.

## Step 1 — Shared foundation (½ day)
1. Create `backend/src/lib/pdf/tokens.ts` (colors/typography from proposal §2–3) and `base-layout.ts` (`renderPdfShell`) — port from `pdf-audit/proposed-template/invoice-template.ts`.
2. Embed assets: logo base64 loader (copy `global-health-dark.png` into `backend/assets/brand/`), bundled body font (Inter/Public Sans woff2) + AlexBrush via `@font-face`; delete the Google Fonts `@import`.
3. Extend `INVOICE_LABELS` with the few new strings (Subtotal, VAT, exemption short note, Payment due, Refund issued, Shipping).

## Step 2 — Fiscal documents (½–1 day)
1. Rewrite `buildInvoiceHtml` in `invoice-pdf.ts` on the shell: all four types (INVOICE / RECEIPT / INVOICE_RECEIPT / CREDIT_NOTE).
2. Fix bugs while there: transposed green `#1B4D3E`, credit-note "Paid" line → "Refund issued", untranslated "Shipping", locale-aware `Intl`/dates/`lang`.
3. Verify: `gen-sample-pdfs.ts` all 4 types × all 5 locales; check long CZ/RO labels; email-attachment smoke test on staging.
4. No fiscal logic changes — numbering, data fetch, sending untouched.

## Step 3 — Clinical HTML templates (1 day)
1. Rebuild the 5 templates in `assets/document-templates/_default/` on the shell: header, patient/consultation columns, doctor panel, body, signature+stamp block, QR + document-ID verification strip (fix `globalhealth.com` → `myglobalhealth.online`), confidentiality footer.
2. Normal type scale (kill 17pt body), print document ID + issue date always.
3. Localise labels per country (shared label map) so the fallback path isn't English-only.
4. Verify per type × locale via extended sample script; test a long prescription for page-break behavior.

## Step 4 — DOCX path convergence (decision + 1–2 days, separate PR)
Option A (recommended, proposal §10.5): make the HTML renderer primary for all countries, keep DOCX as emergency fallback, later delete LibreOffice dependency.
Option B (minimal): keep DOCX primary, restyle the 15 .docx templates to match (logo, type scale, signature block) and recompress the 623KB embedded logo.
Needs your call — A changes the rendering path for CZ/PT/ES/RO/IE clinical docs, so schedule after fiscal + fallback ship cleanly.

## Step 5 — Report PDF exports (½ day, low priority)
Wrap `renderReportPdf` output in the shell (header/footer only, table styling from tokens). Internal-facing.

## Step 6 — External surfaces (config, no code)
- Stripe dashboard → Branding: upload logo, set accent `#1D4B36` (subscription invoices + receipts).
- InvoiceExpress account settings: upload logo/brand color for PT invoices.
- Optional: restyle frontend `/print/order-invoices/<id>` page with the same tokens.

## Step 7 — Regression corpus
Extend `scripts/gen-sample-pdfs.ts`: every type × every locale (+ DOCX path when soffice available); keep `pdf-audit/` previews regenerable via `pdf-audit/gen-previews.ts`.

## Out of scope permanently
Doctor payout invoices, patient uploads, Stripe-hosted pages, national portal documents — not generated by us.
