# Railway backend service — set Root Directory to `backend`.
# Uses Nixpacks (see backend/railway.json). Prisma migrate runs on start.
#
# LibreOffice: branded DOCX → PDF (IE, PT, ES, CZ, RO).
# Playwright: HTML PDF fallback when no DOCX pack (e.g. Brazil).
# postinstall syncs Templates → assets/docx-templates and Alex Brush font.

[phases.setup]
nixPkgs = ["nodejs_22", "pnpm"]
aptPkgs = [
  "libreoffice",
  "libreoffice-writer",
  "fontconfig",
  "fonts-liberation",
  "libnss3",
  "libatk1.0-0",
  "libatk-bridge2.0-0",
  "libcups2",
  "libdrm2",
  "libxkbcommon0",
  "libxcomposite1",
  "libxdamage1",
  "libxfixes3",
  "libxrandr2",
  "libgbm1",
  # Ubuntu Noble (24.04) renamed libasound2 → libasound2t64 (t64 ABI transition).
  # The Railway/nixpacks base image is now Noble, so the old name has no install
  # candidate and the build fails. See: time_t 64-bit transition.
  "libasound2t64",
  "libpango-1.0-0",
  "libcairo2",
]

[phases.install]
cmds = [
  # Monorepo: the pnpm-lock.yaml lives at the repo root, but Railway builds
  # this service with Root Directory = backend, so the lockfile isn't in the
  # build context. --frozen-lockfile therefore fails (ERR_PNPM_NO_LOCKFILE).
  # Use --no-frozen-lockfile (the proven-good value before 459c71d).
  "pnpm install --no-frozen-lockfile",
  "npx playwright install chromium",
]

[phases.build]
cmds = ["pnpm run build"]
