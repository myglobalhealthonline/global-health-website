# Alex Brush (signature font)

Place `AlexBrush-Regular.ttf` here, or set `ALEX_BRUSH_FONT_PATH` to your `.ttf` file.

Bundled copy is synced on `pnpm install` via `scripts/sync-alex-brush-font.mjs` (also checks `~/Downloads/alex-brush/`).
