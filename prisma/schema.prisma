// Database models TBD — wire `DATABASE_URL` in `.env` before running Prisma CLI.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
