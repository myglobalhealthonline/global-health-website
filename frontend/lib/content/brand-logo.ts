/** Default lockup when no `site-logo` CMS asset is configured. */
export const DEFAULT_BRAND_LOGO = {
  src: "/logos/global-health-official.png",
  alt: "Global Health — Medicine anytime anywhere",
} as const;
